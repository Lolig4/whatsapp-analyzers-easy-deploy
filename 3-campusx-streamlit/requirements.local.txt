# Local dependencies (original: requirements.txt, left untouched)
#
# Changes compared to the original:
#   - numpy ADDED : used indirectly via matplotlib/pandas, now explicit.
# emoji stays unpinned (current version); in exchange helper.py:88 was moved
# from the removed API emoji.UNICODE_EMOJI['en'] to emoji.EMOJI_DATA.

streamlit
pandas
numpy
matplotlib
seaborn
wordcloud
urlextract
emoji
