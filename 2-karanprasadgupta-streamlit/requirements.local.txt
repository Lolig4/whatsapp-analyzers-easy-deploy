# Local dependencies (original: requirements.txt, left untouched)
#
# Changes compared to the original:
#   - numpy ADDED : imported by main.py but missing from requirements.txt
#                   (it only came along as a transitive pandas dependency).
# Otherwise identical - the code already uses the current emoji API
# (emoji.EMOJI_DATA), so no version pins are needed.

streamlit
pandas
numpy
matplotlib
seaborn
wordcloud
urlextract
emoji
